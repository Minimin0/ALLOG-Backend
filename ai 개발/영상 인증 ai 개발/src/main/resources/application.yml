spring:
  application:
    name: allog-be
  datasource:
    url: jdbc:mysql://localhost:3306/allog
    username: allog
    password: changeme
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

allog:
  vision:
    anthropic-api-key: ${ANTHROPIC_API_KEY:}
    model: claude-sonnet-5
