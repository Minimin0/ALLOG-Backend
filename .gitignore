# Environment variables
.env
.env.*
!.env.example

# Build outputs
build/
target/
out/
bin/

# Gradle
.gradle/

# Maven
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Logs
*.log
logs/

# Database
*.db
*.sqlite

# Secrets
*.pem
*.key
*.p12
*.jks
# Firebase Android client config. It belongs to the app, not the server, and it
# sits in this working tree only because E2E needs it nearby - keeping it
# untracked by convention alone is one `git add .` away from being committed.
google-services.json

# Local R&D calibration sources. Curated fixtures live in
# src/test/resources/verification/evaluation/ instead.
ai 개발/calibration-images/
ai 개발/test video/

# 배포 시크릿. 템플릿만 추적하고 채워진 값은 절대 커밋하지 않는다.
deploy/allog.env
deploy/*.json
