package com.allog.routine.repository;

import com.allog.routine.domain.RoutineDefinition;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoutineDefinitionRepository extends JpaRepository<RoutineDefinition, Long> {
}
