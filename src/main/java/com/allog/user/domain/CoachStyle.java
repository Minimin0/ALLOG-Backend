package com.allog.user.domain;

/** Tone preference for coach copy. Never an authority over AI safety or business policy. */
public enum CoachStyle {
    SUPPORTIVE,
    PRESSURING,
    FACT_BASED,
    HUMOROUS
}
